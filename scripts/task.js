class Task {
  constructor(title, desc, startDate, dueDate, color, category) {
    this.title = title;
    this.desc = desc;
    this.startDate = startDate;
    this.dueDate = dueDate;
    this.color = color;
    this.category = category;

    this.owner="Albert";
    
  }
}
